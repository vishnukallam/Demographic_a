{
  "name": "social-map-app",
  "version": "1.0.0",
  "description": "Social Map Application (MERN)",
  "license": "MIT",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build && rm -rf ../dist && mv dist ../dist",
    "dev": "concurrently \"npm start\" \"cd client && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.0",
    "cors": "^2.8.5",
    "csv-parser": "^3.2.0",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  },
  "engines": {
    "node": ">=18.x"
  }
}
